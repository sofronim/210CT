def perfect_square(parameter):  
    for number in range (0,parameter):  
	        if number**2 == parameter:  
	            return (number**2)  
	        elif number**2 > parameter:  
	            return (number-1) * (number-1)  
              
                  PSEUDOCODE  
              PERFECT_SQUARE(PARAMETER)  
       for number in range (1,parameter)
          if number**2 = parameter
	            return (number**2)  
	        elif number**2 > parameter  
	            return (number-1) * (number-1)  
